server:
  port: 8899

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/bilgeadamankaraboostjava3/gitconfiglist"
          default-label: main
          skip-ssl-validation: true
          username: "bilgeadamankaraboostjava3"
          password: ${git_password}

